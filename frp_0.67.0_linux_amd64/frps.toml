# FRP 服务端配置示例
# 配合 frps-panel 插件使用

bindPort = 7000

# 注册 frps-panel 插件
[[httpPlugins]]
name = "frps-panel"
addr = "127.0.0.1:7200"
path = "/handler"
ops = ["Login", "NewProxy", "CloseProxy", "Ping"]
