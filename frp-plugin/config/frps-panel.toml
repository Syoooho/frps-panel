# FRP 插件配置 (v2.0.0)
[common]
# 插件监听地址和端口
plugin_addr = "127.0.0.1"
plugin_port = 7200

# 后端 API 地址
backend_url = "http://127.0.0.1:8000"

# 启用 TLS
tls_mode = false
#tls_cert_file = "cert.crt"
#tls_key_file = "cert.key"

